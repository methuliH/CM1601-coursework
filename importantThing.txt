// Sidebar nav buttons — one per tab, clicking calls showTab() to switch views
@FXML
private Button inventoryBtn, searchBtn, cartBtn, dealersBtn;

// The StackPane in the center that holds all 5 tab VBoxes stacked on top of each other
@FXML
private StackPane contentArea;

// The 4 original tab containers — only one is set visible at a time via showTab()
@FXML
private VBox inventoryTab, searchTab, cartTab, dealersTab;

// Stats shown in the low-stock panel: total item count, total inventory value, low-stock count
@FXML
private Label totalItemsLabel, totalValueLabel, lowStockLabel;

// Inventory tab action buttons — Add Part opens the dialog; Export is currently unused (dead field, no matching FXML button anymore)
@FXML
private Button addPartBtn, exportBtn;

// The main table on the Inventory tab, displaying all Part objects as rows
@FXML
private TableView<Part> inventoryTable;

// Inventory table columns — each bound via PropertyValueFactory to a Part getter (code, name, brand, category, price, qty, dateAdded)
@FXML
private javafx.scene.control.TableColumn<Part, String> codeColumn;
@FXML
private javafx.scene.control.TableColumn<Part, String> nameColumn;
@FXML
private javafx.scene.control.TableColumn<Part, String> brandColumn;
@FXML
private javafx.scene.control.TableColumn<Part, String> categoryColumn;
@FXML
private javafx.scene.control.TableColumn<Part, Double> priceColumn;
@FXML
private javafx.scene.control.TableColumn<Part, Integer> quantityColumn;
@FXML
private javafx.scene.control.TableColumn<Part, String> dateAddedColumn;

// New sidebar/action buttons: Audit Logs nav button, Update/Delete selected part, Save low-stock threshold
@FXML
private Button auditBtn, updateSelectedBtn, deleteSelectedBtn, saveThresholdBtn;

// Audit Logs tab container, and the VBox inside the low-stock panel where individual low-stock item rows get added dynamically
@FXML
private VBox auditTab, lowStockListBox;

// Text field where the user types a new low-stock threshold value before clicking Save
@FXML
private TextField lowStockThresholdField;

// Table on the Audit Logs tab, showing logged actions (add/update/delete/checkout)
@FXML
private TableView<Part> auditTable;

// Audit table columns: when the action happened, what action was taken, which part code, and quantity involved
@FXML
private TableColumn<Part, String> auditTimestampColumn, auditActionColumn, auditCodeColumn, auditQuantityColumn;